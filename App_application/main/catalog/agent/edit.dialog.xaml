<Dialog xmlns="clr-namespace:A2v10.Xaml;assembly=A2v10.Xaml"
		Title="Agent">
	<Dialog.Buttons>
		<Button Content="SaveAndClose" Command="{BindCmd SaveAndClose, ValidRequired=True}"/>
		<Button Content="Cancel" Command="{BindCmd Close}"/>
	</Dialog.Buttons>
	<Grid>
		<TextBox Label="Name" Value="{Bind Agent.Name}"/>
		<TextBox Label="Code" Value="{Bind Agent.Code}" Number="True"/>
		<TextBox Label="Memo" Value="{Bind Agent.Memo}" Multiline="True"/>
	</Grid>
</Dialog>