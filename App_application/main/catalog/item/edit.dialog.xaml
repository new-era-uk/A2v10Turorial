<Dialog xmlns="clr-namespace:A2v10.Xaml;assembly=A2v10.Xaml"
		Title="Item">
	<Dialog.Buttons>
		<Button Content="SaveAndClose" Command="{BindCmd SaveAndClose, ValidRequired=True}"/>
		<Button Content="Cancel" Command="{BindCmd Close}"/>
	</Dialog.Buttons>
	<Grid>
		<TextBox Label="Name" Value="{Bind Item.Name}"/>
		<TextBox Label="Article" Value="{Bind Item.Article}" Width="5rem"/>
		<TextBox Label="Memo" Value="{Bind Item.Memo}" Multiline="True"/>
	</Grid>
</Dialog>